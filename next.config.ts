import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Verbergt de dev-indicator zodat hij de onderbalk-navigatie niet overlapt
  devIndicators: false,
};

export default nextConfig;
